	<p class="comment_body"><%= comment.msg %></p>
	<p class="comment_time"><%= time_ago_in_words(comment.updated_at) %> Ago</p>
	<p>
	<% if user_signed_in? %>
	  <% if current_user == @post.user %>			
	  	<%= link_to 'Delete', [comment.post, comment],method: :delete, class: "button", data: { confirm: 'Are you sure?' } %>
		<% end %>
	<% end -%>
	</p>
